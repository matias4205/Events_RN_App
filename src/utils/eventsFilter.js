export const homeworkFilter = ({ type }) => {
    return type === "homework";
}

export const testFilter = ({ type }) => {
    return type === "test";
}

export const scheduleFilter = ({ type }) => {
    return type === "schedule";
}